{
  "name": "hypercycles",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "budo src/index.js:hypercycles.js --live --open --dir dist",
    "build": "browserify src/index.js -o dist/hypercycles.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "git subtree push --prefix dist origin gh-pages"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "budo": "^11.7.0"
  },
  "dependencies": {
    "chroma-js": "^2.4.2"
  }
}
