{
  "name": "hypercycles",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "budo src/index.js:bundle.js --live --open --dir dist",
    "build": "browserify src/index.js -o dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "budo": "^11.7.0"
  }
}
